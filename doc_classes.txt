
World(world.py) = основной класс игры.
    Сохраняет основные параметры, номер игры, хода, год, дату создания игры.
    Список династий и игроков(id)

Dynasty(dynasty.py) = один из основных классов, династия управляемая игроком.

Settlement(settlement.py) = Поселение игрока. К нему создаются экземпляры классов товаров и построек.

Goods(goods.py) = товары. Создается экземпляр к поселению игрока.
    Присутствует словарь, где ключ это название товара, а значение это баланс в поселении.
    Так же в класса есть порсто список всех товаров и их цен.

Buildings(buildings.py) = постройки. Создается экземпляр к поселению игрока.
    Присутствует словарь, где ключ это название постройки, а значение это количество в поселении.
    Так же в класса есть порсто список всех построек и их цен.

Decision(decision.py) =  Решения игрока. Будет иметь набор методов, и их цену.
    ??? Создавать по экземпляру к поселению/игроку или оставить общим?

Cities(cities.py) = Тут можно разместить нейтральные поселения для взаимодействия с ними.
Events(events.py) = События.






############################################################################################

World(world.py) = основной класс игры.
    Сохраняет основные параметры, номер игры, хода, год, дату создания игры.
    Список династий и игроков(id)
    Тут же сохраняем цену построек, высчитывается в конце хода.

5. Cities(cities.py) = статичный класс, не требует сохранения.
    Сохраняется только список городов для их отображения.
    Возможно добавятся модификаторы на цену товаров, что потребует сохранения.

6. Events(events.py) = События. Хз пока че как.

7. Settlement = класс города. Тут должен быть свой экземпляр класса товаров, для определения цен внутри города.

8. Decision =  Решения игрока, типо купить титул. Будет иметь набор методов, количество принятых и их цену
